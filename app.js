import {bfs} from '/algorithms';

TOTAL_ROWS = 20;
TOTAL_COLS = 50;

export default class PathfindingVisualizer{
    constructor(){
        this.state = {
            grid: [],
            mouseIsPressed: false,
        };
    }

    
}
